# Example configuration for Region API tests
# Copy this file to .env in the /test dir and modify values as needed
#
# NOTE: Variable names use environment prefixes (DEV_/UAT_) for consistency with GitHub Actions.
# For local testing, set the ENVIRONMENT variable to choose which config to use (defaults to 'dev').

# Environment selection (dev or uat)
ENVIRONMENT=dev

# DEV Environment Configuration
# Base URL for the Dev Region API server
DEV_API_BASE_URL=http://localhost:8080

# Authentication token for Dev API requests
# This is a service token, which you can create on console, no need to add bearer, it will add it for you
DEV_API_AUTH_TOKEN=your-dev-test-token-here

# Dev test data configuration
# Advised you create all the below in console first and add them all here
DEV_TEST_ORG_ID=test-org-12345
DEV_TEST_PROJECT_ID=test-project-67890

# You can call the /api/v1/organizations/{{organizationID}}/regions endpoint
DEV_TEST_REGION_ID=test-region-abc123
DEV_TEST_SECONDARY_PROJECT_ID=test-secondary-project-12345
DEV_TEST_SECONDARY_REGION_ID=test-secondary-region-67890

# UAT Environment Configuration
# Base URL for the UAT Region API server
UAT_API_BASE_URL=http://localhost:8080

# Authentication token for UAT API requests
UAT_API_AUTH_TOKEN=your-uat-test-token-here

# UAT test data configuration
UAT_TEST_ORG_ID=test-uat-org-12345
UAT_TEST_PROJECT_ID=test-uat-project-67890
UAT_TEST_REGION_ID=test-uat-region-abc123
UAT_TEST_SECONDARY_PROJECT_ID=test-uat-secondary-project-12345
UAT_TEST_SECONDARY_REGION_ID=test-uat-secondary-region-67890

# Test timeout settings
REQUEST_TIMEOUT=30s
TEST_TIMEOUT=20m

# Test behavior flags
SKIP_INTEGRATION=false

# Debug settings
DEBUG_LOGGING=false
LOG_REQUESTS=true
LOG_RESPONSES=false
