---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: filestoragesnapshots.region.unikorn-cloud.org
spec:
  group: region.unikorn-cloud.org
  names:
    categories:
    - unikorn
    kind: FileStorageSnapshot
    listKind: FileStorageSnapshotList
    plural: filestoragesnapshots
    singular: filestoragesnapshot
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels['unikorn-cloud\.org/name']
      name: display name
      type: string
    - jsonPath: .spec.fileStorageID
      name: file storage
      type: string
    - jsonPath: .status.conditions[?(@.type=="Available")].reason
      name: status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          FileStorageSnapshot records customer intent and observed lifecycle state for
          one manual snapshot of a File Storage.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FileStorageSnapshotSpec is the capture request plus mutable
              reconciliation and user metadata.
            properties:
              expirationTime:
                description: |-
                  ExpirationTime optionally requests automatic expiration. Omission means
                  that the snapshot does not expire automatically.
                format: date-time
                type: string
              fileStorageID:
                description: FileStorageID identifies the immutable parent File Storage.
                format: uuid
                type: string
              name:
                description: Name is the immutable, case-sensitive Manual Snapshot
                  Name.
                maxLength: 63
                pattern: ^[A-Za-z0-9]([A-Za-z0-9_.-]*[A-Za-z0-9])?$
                type: string
              pause:
                description: Pause inhibits reconciliation.
                type: boolean
              protectedPath:
                description: |-
                  ProtectedPath is the relative path captured by the snapshot.
                  Omission selects the File Storage root.
                maxLength: 1024
                pattern: ^([^/]+/)*[^/]+$
                type: string
                x-kubernetes-validations:
                - message: protectedPath must not contain . or .. path components
                  rule: self.split('/').all(component, component != '.' && component
                    != '..')
              tags:
                description: Tags are arbitrary user-managed key/value metadata.
                items:
                  description: Tag is an arbirary key/value.
                  properties:
                    name:
                      description: Name of the tag.
                      type: string
                    value:
                      description: Value of the tag.
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
            required:
            - fileStorageID
            - name
            type: object
            x-kubernetes-validations:
            - message: name is immutable
              rule: self.name == oldSelf.name
            - message: fileStorageID is immutable
              rule: self.fileStorageID == oldSelf.fileStorageID
            - message: expirationTime is immutable
              rule: has(self.expirationTime) == has(oldSelf.expirationTime) && (!has(self.expirationTime)
                || self.expirationTime == oldSelf.expirationTime)
            - message: protectedPath is immutable
              rule: has(self.protectedPath) == has(oldSelf.protectedPath) && (!has(self.protectedPath)
                || self.protectedPath == oldSelf.protectedPath)
          status:
            properties:
              absoluteProtectedPath:
                description: |-
                  AbsoluteProtectedPath is the exact path returned by the provider. Writers
                  must preserve it byte-for-byte and must not clear a known value on an
                  incomplete or failed observation.
                type: string
              backendObservedAt:
                description: |-
                  BackendObservedAt records the first definite matching backend observation.
                  It is internal, write-once lifecycle state and is never publicly projected.
                format: date-time
                type: string
              conditions:
                description: Conditions are the sanitized lifecycle and health state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              snapshotTime:
                description: SnapshotTime is the provider-observed capture time.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: backendObservedAt is write-once
          optionalOldSelf: true
          rule: '!oldSelf.hasValue() || !has(oldSelf.value().status) || !has(oldSelf.value().status.backendObservedAt)
            || (has(self.status) && has(self.status.backendObservedAt) && self.status.backendObservedAt
            == oldSelf.value().status.backendObservedAt)'
    served: true
    storage: true
    subresources:
      status: {}
