---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: loadbalancers.region.unikorn-cloud.org
spec:
  group: region.unikorn-cloud.org
  names:
    categories:
    - unikorn
    kind: LoadBalancer
    listKind: LoadBalancerList
    plural: loadbalancers
    singular: loadbalancer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Available")].reason
      name: status
      type: string
    - jsonPath: .status.vipAddress
      name: vipAddress
      type: string
    - jsonPath: .status.publicIP
      name: publicIP
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadBalancer defines a network scoped layer 4 load balancer.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              listeners:
                description: Listeners is the set of listeners exposed by the load
                  balancer.
                items:
                  properties:
                    allowedCIDRs:
                      description: AllowedCIDRs is an optional list of source CIDRs
                        that may access the listener.
                      items:
                        pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\/(?:3[0-2]|[1-2]?[0-9])$
                        type: string
                      type: array
                    idleTimeoutSeconds:
                      description: IdleTimeoutSeconds is the optional TCP idle timeout.
                      maximum: 86400
                      minimum: 1
                      type: integer
                    name:
                      description: Name is the listener name.
                      maxLength: 63
                      pattern: ^[a-z]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    pool:
                      description: Pool is the listener backend pool.
                      properties:
                        healthCheck:
                          description: HealthCheck is the optional health check definition.
                          properties:
                            healthyThreshold:
                              description: HealthyThreshold is the consecutive healthy
                                threshold.
                              maximum: 10
                              minimum: 1
                              type: integer
                            intervalSeconds:
                              description: IntervalSeconds is the health check interval.
                              maximum: 300
                              minimum: 1
                              type: integer
                            timeoutSeconds:
                              description: TimeoutSeconds is the health check timeout.
                              maximum: 300
                              minimum: 1
                              type: integer
                            unhealthyThreshold:
                              description: UnhealthyThreshold is the consecutive unhealthy
                                threshold.
                              maximum: 10
                              minimum: 1
                              type: integer
                          required:
                          - healthyThreshold
                          - intervalSeconds
                          - timeoutSeconds
                          - unhealthyThreshold
                          type: object
                          x-kubernetes-validations:
                          - message: timeoutSeconds must be less than intervalSeconds
                            rule: self.timeoutSeconds < self.intervalSeconds
                        members:
                          description: Members is the set of pool members.
                          items:
                            properties:
                              address:
                                description: Address is the member IP address.
                                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])$
                                type: string
                              port:
                                description: Port is the member port.
                                maximum: 65535
                                minimum: 1
                                type: integer
                            required:
                            - address
                            - port
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - address
                          - port
                          x-kubernetes-list-type: map
                        proxyProtocolV2:
                          description: ProxyProtocolV2 enables Proxy Protocol v2.
                          type: boolean
                      required:
                      - members
                      type: object
                    port:
                      description: Port is the listener port.
                      maximum: 65535
                      minimum: 1
                      type: integer
                    protocol:
                      description: Protocol is the listener protocol.
                      enum:
                      - tcp
                      - udp
                      type: string
                  required:
                  - name
                  - pool
                  - port
                  - protocol
                  type: object
                  x-kubernetes-validations:
                  - message: idleTimeoutSeconds is only supported for TCP listeners
                    rule: self.protocol == 'tcp' || !has(self.idleTimeoutSeconds)
                  - message: proxyProtocolV2 is only supported for TCP listeners
                    rule: self.protocol == 'tcp' || self.pool.proxyProtocolV2 == false
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              pause:
                description: Pause, if true, will inhibit reconciliation.
                type: boolean
              publicIP:
                description: PublicIP controls whether a public IP should be allocated.
                type: boolean
              requestedVIPAddress:
                description: RequestedVIPAddress is the optional requested virtual
                  IP address.
                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])$
                type: string
              tags:
                description: |-
                  Tags are an abitrary list of key/value pairs that a client
                  may populate to store metadata for the resource.
                items:
                  description: Tag is an arbirary key/value.
                  properties:
                    name:
                      description: Name of the tag.
                      type: string
                    value:
                      description: Value of the tag.
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
            type: object
          status:
            properties:
              conditions:
                description: Current service state of a load balancer.
                items:
                  description: |-
                    Condition is a generic condition type for use across all resource types.
                    It's generic so that the underlying controller-manager functionality can
                    be shared across all resources.
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      enum:
                      - Provisioning
                      - Provisioned
                      - Cancelled
                      - Errored
                      - Deprovisioning
                      - Deprovisioned
                      - Unknown
                      - Healthy
                      - Degraded
                      type: string
                    status:
                      description: |-
                        Status is the status of the condition.
                        Can be True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      enum:
                      - Available
                      - Healthy
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              publicIP:
                description: PublicIP is the provisioned public IP address.
                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])$
                type: string
              vipAddress:
                description: VIPAddress is the provisioned virtual IP address.
                pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])$
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
