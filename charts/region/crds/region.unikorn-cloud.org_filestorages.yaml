---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: filestorages.region.unikorn-cloud.org
spec:
  group: region.unikorn-cloud.org
  names:
    categories:
    - unikorn
    kind: FileStorage
    listKind: FileStorageList
    plural: filestorages
    singular: filestorage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Available")].reason
      name: status
      type: string
    - jsonPath: .spec.size
      name: size
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FileStorage defines a FileStorageSpec.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FileStorageSpec defines the storage request.
            properties:
              attachments:
                description: Attachments are the network attachments for the storage.
                items:
                  description: Attachment has the network identifier for the storage.
                  properties:
                    ipRange:
                      description: IPRange is the IP range for the attachment.
                      properties:
                        endIP:
                          description: End is the end IP address for the attachment.
                          pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])$
                          type: string
                        startIP:
                          description: Start is the start IP address for the attachment.
                          pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])$
                          type: string
                      required:
                      - endIP
                      - startIP
                      type: object
                    networkID:
                      description: NetworkID is the network ID for the attachment.
                      type: string
                    segmentationID:
                      description: SegmentationID is the VLAN ID for the attachment.
                      maximum: 4094
                      minimum: 1
                      type: integer
                  required:
                  - networkID
                  type: object
                type: array
              defaultSnapshotProtectionEnabled:
                description: DefaultSnapshotProtectionEnabled controls platform-managed
                  baseline snapshot protection.
                type: boolean
              nfs:
                description: NFS is fulfilled when leveraging the NFS storage class.
                properties:
                  rootSquash:
                    type: boolean
                type: object
              pause:
                description: Pause, if true, will inhibit reconciliation.
                type: boolean
              size:
                anyOf:
                - type: integer
                - type: string
                description: Size is the total size of the file storage.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              snapshotPolicies:
                description: SnapshotPolicies are the named snapshot protection rules
                  for the storage.
                items:
                  properties:
                    name:
                      description: Name is the policy identity key within the file
                        storage volume.
                      maxLength: 19
                      pattern: ^[a-z]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    retention:
                      description: Retention defines how many snapshots are retained.
                      properties:
                        keep:
                          description: Keep is the number of snapshots to retain.
                          minimum: 1
                          type: integer
                      required:
                      - keep
                      type: object
                    schedule:
                      description: Schedule defines when snapshots run in UTC.
                      properties:
                        dayOfMonth:
                          description: DayOfMonth is the month day for monthly schedules.
                          maximum: 28
                          minimum: 1
                          type: integer
                        dayOfWeek:
                          description: DayOfWeek is the weekday for weekly schedules.
                          enum:
                          - monday
                          - tuesday
                          - wednesday
                          - thursday
                          - friday
                          - saturday
                          - sunday
                          type: string
                        interval:
                          description: Interval is the snapshot schedule cadence.
                          enum:
                          - hourly
                          - daily
                          - weekly
                          - monthly
                          type: string
                        timeOfDay:
                          description: TimeOfDay is the UTC time for daily, weekly,
                            and monthly schedules.
                          pattern: ^([01][0-9]|2[0-3]):[0-5][0-9]Z$
                          type: string
                      required:
                      - interval
                      type: object
                      x-kubernetes-validations:
                      - message: hourly schedules must not define timeOfDay, dayOfWeek,
                          or dayOfMonth
                        rule: 'self.interval == ''hourly'' ? !has(self.timeOfDay)
                          && !has(self.dayOfWeek) && !has(self.dayOfMonth) : true'
                      - message: daily schedules require timeOfDay and must not define
                          dayOfWeek or dayOfMonth
                        rule: 'self.interval == ''daily'' ? has(self.timeOfDay) &&
                          !has(self.dayOfWeek) && !has(self.dayOfMonth) : true'
                      - message: weekly schedules require timeOfDay and dayOfWeek
                          and must not define dayOfMonth
                        rule: 'self.interval == ''weekly'' ? has(self.timeOfDay) &&
                          has(self.dayOfWeek) && !has(self.dayOfMonth) : true'
                      - message: monthly schedules require timeOfDay and dayOfMonth
                          and must not define dayOfWeek
                        rule: 'self.interval == ''monthly'' ? has(self.timeOfDay)
                          && has(self.dayOfMonth) && !has(self.dayOfWeek) : true'
                  required:
                  - name
                  - retention
                  - schedule
                  type: object
                maxItems: 5
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              storageClassID:
                description: StorageClassID is the storage class ID.
                type: string
              tags:
                description: |-
                  Tags are an abitrary list of key/value pairs that a client
                  may populate to store metadata for the resource.
                  Tags are aribrary user data.
                items:
                  description: Tag is an arbirary key/value.
                  properties:
                    name:
                      description: Name of the tag.
                      type: string
                    value:
                      description: Value of the tag.
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
            required:
            - size
            - storageClassID
            type: object
          status:
            properties:
              attachments:
                description: Attachments reflects the observed attachment state per
                  network.
                items:
                  properties:
                    ipRange:
                      description: |-
                        IPRange is the currently provisioned/observed IP range for the attachment.
                        (May differ from attachments[].ipRange while provisioning/resizing.)
                      properties:
                        endIP:
                          description: End is the end IP address for the attachment.
                          pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])$
                          type: string
                        startIP:
                          description: Start is the start IP address for the attachment.
                          pattern: ^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9]?[0-9])$
                          type: string
                      required:
                      - endIP
                      - startIP
                      type: object
                    message:
                      description: Human-readable message indicating details about
                        the attachment.
                      type: string
                    networkID:
                      description: NetworkID is the network ID for the attachment.
                      type: string
                    provisioningStatus:
                      description: ProvisioningStatus indicates if this attachment
                        is ready/failed/etc.
                      enum:
                      - Provisioning
                      - Provisioned
                      - Errored
                      - Deprovisioning
                      type: string
                    segmentationID:
                      description: SegmentationID is the VLAN ID for the attachment.
                      type: integer
                  required:
                  - message
                  - networkID
                  - provisioningStatus
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - networkID
                x-kubernetes-list-type: map
              conditions:
                description: Current service state of a file storage.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              mountPath:
                description: MountPath is the path where the file storage is mounted.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              size:
                anyOf:
                - type: integer
                - type: string
                description: |-
                  Size is the currently provisioned/observed size of the file storage.
                  (May differ from spec.size while provisioning/resizing.)
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              snapshotPolicies:
                description: SnapshotPolicies reflects the observed state for each
                  desired snapshot policy.
                items:
                  properties:
                    conditions:
                      description: Conditions reflect reconciliation state for this
                        snapshot policy.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    name:
                      description: Name is the policy identity key from spec.
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              usage:
                anyOf:
                - type: integer
                - type: string
                description: Usage is the amount of storage currently in use.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              usageTimestamp:
                description: UsageTimestamp is the timestamp when the usage was last
                  updated.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
