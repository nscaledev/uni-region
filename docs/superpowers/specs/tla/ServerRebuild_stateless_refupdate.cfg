\* STATELESS decision, but Nova's reported ref flips to the target at
\* ACCEPT time (RefUpdatesAtAccept = TRUE) - the accept-time DB bookkeeping
\* acting as an implicit write-ahead marker. Expected: AcceptBudget
\* HOLDS - showing the stateless design is safe if and only if Nova's
\* bookkeeping is trusted, the exact trust INST-1235 shows misplaced.
SPECIFICATION Spec
CONSTANTS
  LevelWake = TRUE
  UseFence = TRUE
  UsePreArm = TRUE
  RequeueOn409 = TRUE
  UseMarker = FALSE
  RefUpdatesAtAccept = TRUE
INVARIANTS
  TypeOK
  AcceptBudget
CONSTRAINT
  Constraint
